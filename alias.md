```bash
alias make-venv="python3 -m venv .venv && source .venv/bin/activate; cat ~/Documents/System/dotfiles/skeletons/gitignore.txt >> .gitignore"
alias make-require="python3 -m pip freeze > requirements.txt"
alias copy="xclip -selection clipboard"
```
